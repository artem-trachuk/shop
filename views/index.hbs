{{> search}}
<div class="row">
  <div class="col-12 col-lg-3">
    {{#if fields}}
      {{> filters}}
    {{/if}}
    <h4>Категории</h4>
    {{> categories}}
  </div>
  <div class="col-12 col-lg-9">
    {{> products}}
  </div>
</div>